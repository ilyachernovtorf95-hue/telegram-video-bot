{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "/bin/sh -c 'if [ -n \"${RAILWAY_PROJECT_ID:-}\" ] && [ \"$RAILWAY_PROJECT_ID\" != \"0782ee62-74b0-447a-94e3-e88cd24c2e01\" ]; then echo \"STANDBY: Telegram polling disabled for ${RAILWAY_PROJECT_NAME:-unknown} ($RAILWAY_PROJECT_ID)\"; exec tail -f /dev/null; fi; echo \"PRIMARY: starting lightweight responsive Telegram dispatcher\"; exec python /app/bot_runner.py'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "overlapSeconds": 0,
    "drainingSeconds": 5
  }
}
